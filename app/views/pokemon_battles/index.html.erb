<!-- - Page Header -->
<header class="masthead">
  <div class="overlay"></div>
  <div class="container">
    <div class="row">
      <div class="col-md-4x">
        <div class="site-heading">
          <h1>Pokemon Battles</h1>
        </div>
      </div>
    </div>
  </div>
</header>
<hr>
<div class="container">
  <% if notice.present? %>
    <p id="notice" class="alert alert-success"><%= notice %></p>
  <% end %>
  <div class="row col-md-4x">
    <%= link_to 'Create', new_pokemon_battle_path, :class=>"btn btn-primary float-left mb-2" %>
    <hr>
    <table class="table table-striped" border="1"">
      <thead class="thead-dark">
        <tr>
          <th>Pokemon 1</th>
          <th>Pokemon 2</th>
          <th>Turn</th>
          <th>Stage</th>
          <th>Pokemon Winner</th>
          <th>Pokemon Loser</th>
          <th colspan="3" class="text-center">Action</th>
        </tr>
      </thead>
      <tbody>
        <% @pokemon_battles.each do |pokemon_battle| %>
          <tr>
            <td><%= %></td>
            <td><%=  %></td>
            <td><%=  %></td>
            <td><%=  %></td>
            <td><%=  %></td>
            <td><%=  %></td>
            <td><%= link_to 'Show', pokemon, :class => "btn btn-primary" %></td>
            <td><%= link_to 'Edit', edit_pokemon_path(pokemon), :class => "btn btn-warning" %></td>
            <td><%= link_to 'Destroy', pokemon, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <div class="digg_pagination float-right">
      <%= will_paginate @pokemon_battles %>
    </div>
  </div>
</div>
